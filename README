# This is the first git file

- Learning Git Diff (with and without --staged)
- Learning to amend a commit
- Learning to reset HEAD <file-name>
- Learning git checkout -- <file-name> to discard changes
- Learning undos with git restore with and without --staged
